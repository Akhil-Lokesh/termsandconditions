[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: Integration tests (require services)
    unit: Unit tests
addopts =
    -v
    --strict-markers
    --tb=short
